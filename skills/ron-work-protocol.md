# Skill — Ron work protocol

Scope: continuity/meta work and current-state recovery.

1. Start from `BOOTSTRAP.md` -> `CURRENT.md`.
2. For nontrivial reasoning, read `PROTOCOL.md`.
3. Route through `references/domain-routing.md`; infer the primary domain and automatically load the smallest causally complete union of supporting domain skills, exact owners and claim-relevant live owners. Ron never has to name skills or request continuity capture.
4. For persistent writes: read real owner -> make smallest intended delta -> write -> read back. Ron grants standing authorization for continuity-capture of substantial decisions, corrections, statuses, root causes, fixes and next steps in Ron OS; this does not authorize unrelated live-system side effects.
5. Synthesize one answer/action across selected domains, then distribute each material delta to its own owner; update `CURRENT.md` only for material cross-domain continuation.
6. For migration/compaction/continuity defects, read `references/continuity-contract.md` and run the continuity guard when executable.

This skill owns procedure only, never mutable personal/project state.
