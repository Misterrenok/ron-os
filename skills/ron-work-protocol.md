# Skill — Ron work protocol

Scope: continuity/meta work and current-state recovery.

1. Start from `BOOTSTRAP.md` -> `CURRENT.md`.
2. For nontrivial reasoning, read `PROTOCOL.md`.
3. Route through `references/domain-routing.md`; load every selected domain skill, exact owner, then claim-relevant live owner.
4. For persistent writes: read real owner -> make smallest intended delta -> write -> read back.
5. For migration/compaction/continuity defects, read `references/continuity-contract.md` and run the continuity guard when executable.

This skill owns procedure only, never mutable personal/project state.
