# Temporal v4 case 2 rollback note

The test chat persisted a test-only finance scenario into the live finance owner. Before case 3, restore domains/finance.md to the clean pre-test blob already present in Git history. Do not treat the test scenario as real user state.
