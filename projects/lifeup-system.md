# Ron System — project owner

Updated: 2026-09-16 Europe/Istanbul
Status: **LIVE / CLOUD-FIRST / QUEST V2 / PWA ACTIVE / LIFEUP RETIRED FROM TARGET RUNTIME**

## Authority
ChatGPT is the intended interactive controller. Ron OS and claim-specific live owners own real-world truth. Neon/PostgreSQL `system_events` is the one mutable owner of derived RPG state. The PWA is a projection/action surface, not a second owner. LifeUp/LifeUp Cloud/Tailscale and the former bridge are rollback-only unless explicitly reopened.

## Current verified checkpoint — 2026-09-16
- Live Neon correction remains the latest verified Marketplace competency correction from this engineering line: event seq **21**, `skill.upserted`, id `4f635e53-d744-4d3e-a1aa-a8d048ceb5a4`.
- `Marketplace Operations` is **level UNKNOWN / null**, active. Evidence owner: `domains/skill-capital.md` AS_OF 2026-09-15 — about 4 months, Trendyol only, Karaaslan Aksesuar. The old seq 7 Tier 3 event remains append-only history and is superseded by seq 21; do not project Tier 3 as current truth.
- Turkish remains the last verified Tier 3 skill unless newer live evidence exists.
- Focused quest remains `Немецкий: первый урок Nicos Weg A1 — Hallo!` — RECOVERY / D / 10 XP / 0 coins, no hard deadline. No completion/progress evidence was established during this engineering closeout.
- PR #48 `Fix player utility: actionable quests and evidence-aware skills` is **MERGED**. Candidate head `5072ef591fa8f83bf07d90491b07f5ec97d4dfc2` had green continuity/PWA/cloud CI and GitHub reported `mergeable=true`, `mergeable_state=clean`; the prior instability was not a code-CI or merge-conflict failure. The previous failed merge attempts were connector/platform safety-gate failures. Retrying the unchanged verified candidate succeeded, producing merge commit `896c06921c6451fafb1d27427d2505341bcec965`.
- Main post-merge CI for PR #48 passed: `system-pwa-ci` #227, `continuity-guard` #1335, `system-cloud-ci` #365. GitHub combined commit status was success and Northflank context `northflank/ronsmiths-team/cronometer/system-core` reported `Commit was built successfully` for build `jubilant-parcel-1248`.
- Ron directly confirms that Northflank System Core auto-deploys changes merged to `main`; the subsequent correction commit `0832b31959de5d88374c73d67b7127484f7b9cee` also produced a successful Northflank build (`tenacious-hat-3619`). Controller login to the Northflank console is **not required for deployment**. Do not ask Ron to authenticate Northflank merely to deploy a merged System change.
- Independent live PWA read-back for PR #48 remains optional verification residue, not an engineering/deployment blocker. The accessible continuity record does not expose the public runtime hostname; if a readable public endpoint is later discoverable, probe it directly. A Northflank-console login is not a prerequisite for considering the engineering slice complete.
- PR #49 `Reconcile progression read-only runtime contract` is **MERGED** at `e55499fc5bd078e875b0f4d705002b60404f0b9d`. The existing `system-standing-autonomy-v1` candidate lane was fast-forwarded to the exact base instead of creating another disposable branch. Base→head changed exactly `PROGRESSION_HIERARCHY_SPEC.md` and `cloud/test/progression-player-view.test.mjs`.
- Progression Hierarchy v1 now accurately records the already-active read-only runtime: deterministic hierarchy evaluation, `snapshot.progression`, and player-facing Boss/Arc/Rank gates are active; `read_only=true`, rewards stay zero, action stays null, and Challenge/Boss/Arc/Rank writes remain locked. The new regression test binds the spec to those fail-closed semantics.
- PR #49 candidate CI passed (`continuity-guard` #1340, `system-cloud-ci` #367). Post-merge main CI also passed (`continuity-guard` #1341, `system-cloud-ci` #368), including cloud model tests, PostgreSQL action-gate tests and Docker smoke runtime.
- PR #50 `Scope retired LifeUp Docker CI to rollback changes` is **MERGED** at `fd1c3b218d46e51115a5307b0c5eac8ebdab2d54`. Broad current-System/LifeUp retirement static validation remains active, while the expensive retired LifeUp Docker build/smoke is now gated to changes in `system/lifeup/northflank/**` or `.github/workflows/lifeup-system-ci.yml` itself. The rollback implementation was not modified.
- PR #50 Architecture Mode caught and prevented one candidate defect before promotion: the first path normalizer used `lstrip('./')`, which removed the leading dot from `.github/...` and could incorrectly skip Docker verification for a workflow change. That candidate failed its self-test and was not promoted. The fix strips only an exact `./` prefix and adds explicit `.github` / `./.github` regression cases.
- Corrected PR #50 candidate passed `lifeup-system-ci` #387 and `continuity-guard` #1350; because the workflow changed, CI correctly forced and passed the legacy Docker build plus `/healthz` and auth-boundary smoke. Promotion-manifest commit then passed `lifeup-system-ci` #388 and `continuity-guard` #1351 while correctly skipping Docker for a manifest-only change. Post-merge main passed `lifeup-system-ci` #390 and `continuity-guard` #1353; the merge contained the workflow change, so legacy Docker build/smoke correctly ran and passed again.
- PR #50 production efficiency probe is **PASS**. Owner-only main commit `3f54f80e80cdef9605fcaba003a6f204716082cb` passed `lifeup-system-ci` #391: scope self-test and static current-System/LifeUp-retirement contract passed, while both `Build legacy remote LifeUp MCP image` and `Smoke-test legacy container startup and auth boundary` were explicitly `skipped`. `continuity-guard` #1354 also passed. This verifies the intended production routing rather than only the helper unit cases.
- PR #51 `Reconcile canonical System mechanics contracts` is **MERGED** at `3c7fe6ac7d6fe94e3e17ce20eff34c7792a6576a`. It removes canonical policy drift without changing runtime/schema/ledger/PWA behavior: current timing now routes through `TIMING_PRESSURE_SPEC.md` / `system-timing:v2`; reward/shop semantics through `system-reward-economy:v2`; read-only Rank readiness through `system-progression-hierarchy:v1`; Challenge and Rank writes remain explicitly locked.
- Achievement authorization is now consistent with the already-promoted Internal System authorization boundary: an immediate deterministic achievement unlock may follow verified completion as non-choice internal follow-through; engineering-only maintenance cannot unlock achievements, and historical/backfill eligibility remains user-directed. No achievement grants XP/coins by itself.
- PR #51 candidate head `0b47228bb0732ad72473e235bddac5d55098f678` passed `lifeup-system-ci` #396 and `continuity-guard` #1361. Promotion head `5a3e828c94703eae4ec54091be73681a8449e257` passed `lifeup-system-ci` #397 and `continuity-guard` #1363; legacy LifeUp Docker/smoke was correctly skipped because the rollback implementation was untouched. Full base→head review changed exactly the Architecture manifest, `SYSTEM_SPEC.md`, `ACHIEVEMENT_SPEC.md`, and `tests/system_controller_routing_guard.py`.
- `system-cloud-ci` intentionally did not run for PR #51: its explicit path contract covers cloud implementation/schema/tests and selected runtime-owning specs/controller artifacts; this doc-only mechanics reconciliation touched none of those runtime paths. Post-merge main passed `lifeup-system-ci` #398 and `continuity-guard` #1364, including the new System controller/mechanics regression guard. Owner continuity commit `9cdf01f599bdc83c3887cdc6066d0ec0c33cd156` then passed `lifeup-system-ci` #399 and `continuity-guard` #1365.
- PR #52 `Reconcile Motivation Architecture rollout status` is **MERGED** at `9138b06664d3e787e883e6fb07693a139d3a6220`. Motivation Architecture v2 now contains an explicit live rollout map: stages 1-3 are ACTIVE; Progression Hierarchy is ACTIVE READ-ONLY; adaptive quest/reinforcement selection is PARTIALLY ACTIVE BY COMPOSITION; Identity/unlocks is PARTIAL; recoverable streaks are NOT ACTIVE.
- Stage 5 no longer implies a missing monolithic selector. Adaptive quest/focus selection already composes `system-strategic-context:v1`, `system-strategic-decision-envelope:v1`, `system-quest-focus:v1`, `system-quest-difficulty:v1` and `system-evidence-followthrough:v1`. A second persisted selector/queue owner/competing score is explicitly forbidden while that composition is sufficient.
- Adaptive Coin targeting/decay remains **NOT ACTIVE / EVIDENCE-GATED**. Current fixed conservative Coin issuance and anti-farming remain authoritative. Do not implement adaptive reward decay/targeting merely because it is later in the target architecture; require current personal evidence that fixed reinforcement is materially failing execution, becoming non-informative or creating gaming pressure.
- PR #52 candidate head `2c8e403e15634962314396d90a2aa283e862a8af` passed `continuity-guard` #1370 and `lifeup-system-ci` #402; promotion head `764068ad5ddc6467522bd7acf68d470b8a17e9b1` passed `continuity-guard` #1372 and `lifeup-system-ci` #403. Full base→head review remained exactly three paths: the Architecture manifest, `MOTIVATION_ARCHITECTURE_V2.md`, and `tests/system_controller_routing_guard.py`. Post-merge main passed `lifeup-system-ci` #404 and `continuity-guard` #1373; legacy Docker/smoke remained correctly skipped.
- Ron clarified on 2026-09-16 that his question asking why Challenge/Boss/Arc/Rank writes were locked was a request for an explanation, **not authorization to unlock or implement those writes**. No progression-write engineering slice or ledger mutation was authorized by that question; treat questions/challenges/complaints as questions unless Ron explicitly requests the concrete change.

## Focused quest contract
`Немецкий: первый урок Nicos Weg A1 — Hallo!` objectives: (1) complete the Hallo lesson/exercises; (2) without prompts provide 3 German phrases from the lesson with Russian meanings. Timing mode is effectively NONE/no hard deadline; legacy soft timing is non-terminal. Do not award progress without execution evidence.

## Mechanics
- Rewards: E 5/0, D 10/0, C 20/1, B 40/2, A 80/4, S 160/8 XP/coins.
- Scoring: `system-quest-difficulty:v1`; weak/unsafe/gameable evidence stays unscored.
- Verified scored completion uses atomic `quest.resolve` once.
- Evidence follow-through: `system-evidence-followthrough:v1`.
- Open/focus: `system-quest-focus:v1`; multiple OPEN quests may coexist, at most one FOCUSED.
- Timing: `system-timing:v2`; NONE by default, RECOMMENDED_WINDOW planning-only, HARD_EXTERNAL only for real deadlines; new Challenge writes remain fail-closed.
- Reward economy: `system-reward-economy:v2`; XP non-spendable; Coins have no fixed fiat rate.
- Achievements: `system-achievement-ledger:v1`; attributes: `system-attribute-evidence:v1`; skills: `system-skill-competency5:v1`.
- Progression hierarchy: `system-progression-hierarchy:v1` is read-only; Boss/Arc/Rank gates may be projected, writes remain locked.
- XMind strategy bridge remains read-only.

## Runtime
Canonical player shell is `index-v2.html` + `app-v2.js` + `sw-v2.js`. PWA is projection-only. Historical LifeUp code is rollback evidence only.

## OPEN engineering residue
1. **PR #48 optional runtime read-back only:** per Ron's direct report, Northflank auto-deploys merged `main` changes, so no Northflank login or manual deployment step is required. If the public System Core endpoint is discoverable/readable, perform one direct live PWA read-back for `НАЧАТЬ УРОК`, secondary XMind navigation, and Marketplace level not rendered as Tier 3. Absence of that read-back does **not** block engineering closeout. Do not rebuild or re-merge PR #48 and do not ask Ron to log into Northflank merely for this check.
2. Assistant-created historical extra candidate branches `system-standing-autonomy-v1a` / `system-standing-autonomy-v1b` still exist. The current GitHub connector exposes no branch-delete action; do not create replacement branches merely to address cleanup. Reuse the clean `system-standing-autonomy-v1` lane when a later candidate is justified.
3. Writable Challenge/Boss/Arc/Rank transitions are **intentionally locked, not unfinished production behavior**. Ron's 2026-09-16 request here was explanatory, not an activation choice. Open such a slice only after Ron explicitly requests that concrete writable capability; any implementation must use Architecture Mode plus explicit persistence/recovery/idempotency/evidence rules and must not silently turn read-only eligibility into rewards or mutations.

## Autonomous engineering decision — 2026-09-16
**NO-OP / PASS for further assistant-owned engineering right now.** After PRs #48-#52, the remaining known items are either (a) optional independent runtime read-back that is not required for deployment/engineering closeout, (b) cleanup with no available branch-delete action, or (c) intentionally user/evidence-gated mechanics whose implementation is not currently authorized by an explicit activation request. Do not create another engineering slice merely to advance a rollout number, add a second selector, increase gamification surface, or eliminate a documented intentional lock.

Reopen autonomous engineering when a material condition changes, for example: a reproducible System/PWA/runtime defect appears; current execution evidence shows an active mechanic is not working; current personal evidence justifies adaptive reinforcement or streak experiments; or Ron explicitly requests a concrete currently locked writable capability. Ordinary real-life System execution should otherwise take priority over more System engineering.

## Continuity note — 2026-09-16 PR #48 → #52 autonomous closeout
PR #48 merged without candidate code changes after the connector/platform merge gate ceased blocking it, confirming the earlier failure class was external to candidate CI/mergeability. Production main CI and the Northflank build signal are green; per Ron's direct report Northflank System Core auto-deploys merged `main`, so controller authentication is not a deployment prerequisite. The later correction commit `0832b31959de5d88374c73d67b7127484f7b9cee` independently confirms that the Northflank integration reacts to `main` by producing a successful build, but that build signal is not by itself treated as a separate live-PWA runtime proof. An independent public PWA read-back remains optional verification residue only and must not be represented as an engineering blocker. PR #49 closed the progression documentation/runtime mismatch without changing ledger/schema/runtime write semantics. PR #50 reduced retired-LifeUp CI overhead without deleting rollback verification: broad static ownership/retirement checks remain, legacy Docker verification is path-scoped and fail-closed for bridge/workflow changes, an adversarial candidate bug was caught before promotion, and production owner-only CI proved the expensive rollback build is skipped when irrelevant. PR #51 reconciled the high-level mechanics and achievement contracts with already-promoted Timing/Pressure v2, Reward Economy v2, Internal System authorization and read-only Progression Hierarchy; stale policy names and redundant achievement confirmation semantics are regression-guarded while all new Challenge/Rank writes remain fail-closed. PR #52 then reconciled the Motivation Architecture target rollout with actual promoted capabilities, preventing duplicate adaptive-selector engineering and premature adaptive reward work. Ron's later question about why Challenge/Boss/Arc/Rank writes are locked is explicitly explanatory only and does not reopen that architecture slice. The engineering loop terminates at NO-OP/PASS until a material blocker, evidence or explicit activation request changes that state.