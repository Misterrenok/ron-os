# ron-os

Canonical, versioned continuity/current-state store for Ron's ChatGPT workflows.

Start at `BOOTSTRAP.md`. `CURRENT.md` is the thin current routing/checkpoint index. Domain files and live applications own their respective mutable state.

ChatGPT Library is legacy/backup after migration; it is not the runtime source of truth.
